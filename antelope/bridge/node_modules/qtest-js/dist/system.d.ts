import { Chain } from "./chain";
import { Account } from "./account";
export declare class System {
    chain: Chain;
    constructor(chain: Chain);
    createAccounts(accounts: string[], supplyAmount?: string): Promise<Account[]>;
    createAccount(account: string, supplyAmount?: string, bytes?: number): Promise<Account>;
    fromAccount(accountName: string): Promise<Account>;
}
