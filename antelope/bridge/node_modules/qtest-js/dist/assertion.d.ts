import { ActionTrace, TransactResult } from "eosjs/dist/eosjs-api-interfaces";
import { Authorization } from "eosjs/dist/eosjs-serialize";
import { Account } from "./account";
import { Asset } from "./asset";
export declare function expectThrow(promise: Promise<any>, message: string): Promise<boolean>;
export declare function getAllActions(actionTraces: ActionTrace[]): {
    receiver: string;
    name: string;
    data?: object;
    authorization?: Authorization[];
}[];
export declare function expectAction(transaction: TransactResult, code: string, actionName: string, data?: object, authorization?: Authorization[]): Promise<boolean>;
export declare function expectBalance(account: Account, expectedBalance: Asset): Promise<void>;
