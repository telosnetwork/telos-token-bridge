import { Account } from "./account";
import { TransactResult } from "eosjs/dist/eosjs-api-interfaces";
import { Table } from "./table";
export declare class Contract {
    wasm: string;
    abi: any;
    account: Account;
    abiSerializer: any;
    action: {
        [key: string]: (data: object, authorization?: [{
            actor: string;
            permission: string;
        }]) => Promise<TransactResult>;
    };
    table: {
        [key: string]: Table;
    };
    constructor(account: any, wasm: any, abi: any);
}
