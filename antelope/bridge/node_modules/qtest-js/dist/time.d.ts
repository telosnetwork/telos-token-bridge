import { Chain } from "./chain";
export declare class Time {
    static timeAdjustDelaySecond: number;
    chain: Chain;
    timeAdded: number;
    constructor(chain: Chain);
    increase(time: number, fromBlockTime?: string): Promise<number>;
    increaseTo(time: number): Promise<number>;
}
